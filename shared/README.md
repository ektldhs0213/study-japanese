# Shared utilities

This directory contains framework-independent utilities that can be copied or
published for both services. Service-specific database access remains inside
each application's `src/services` directory.
